<% provide(:title, @user.username) %>


<% if can?(:read, @profile) and !@profile.full_name.blank? %>
  <%= @profile.full_name %>
  <hr>
<% end %>

<!-- Project List -->

<% project_count = @user && @user.project_memberships.count | 0 %>

<% if @user == current_user and current_user.has_role? :customer %>

  <% if project_count == 0 %>
    <%= link_to new_project_path, :class => 'button' do %>
      <i class='general foundicon-plus'></i>Start Your First Project
    <% end %>
  <% else %>
    
    <ul class="large-block-grid-2">
      <% @user.projects.each do |p| %>
        <li>
          <h4><%= link_to p.title, project_path(p) %></h4>
          <p><%= truncate(p.description, :length => 50) %></p>
        </li>
      <% end %>      
    </ul>

        
      <%= link_to new_project_path, :class => 'button small' do %>
        <i class='general foundicon-plus'></i>New Project
      <% end %>
    

  <% end %>
  
<% else %>

  Welcome to the profile of <%= @user.username %>

<% end %>

<hr />

<!-- Recent Activity -->

<h4>Activity</h4>

<p>No recent activity.</p>

<p>TODO: Recent user activity</p>