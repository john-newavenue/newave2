<% provide(:title, "New Design Example") %>

<% content_for :main do %>

  <h1><%=yield(:title) %></h1>

  <%= semantic_form_for @brochure, :as => 'brochure', :url => brochure_floorplans_path do |f| %>
    <%= f.inputs do %>
      <%= render :partial => 'frontend/content/brochure/form_fields', :locals => { :f => f } %>
      <%= f.action :submit, :as => :button, :label => 'Create', :class => "success button" %>
      <%= link_to "Cancel and go back", brochure_floorplans_path %>
    <% end %>
  <% end %>

<% end %>

<% content_for :sidebar do %>

<% end %>