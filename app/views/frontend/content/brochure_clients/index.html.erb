<% provide(:title, "Client Stories") %>


<% content_for :main do %>

  <h1><%=yield(:title) %></h1>

  <p>
    Our clients have a unique set of needs, goals, and ideas for their small home or backyard cottage and our top priority is to understand the unique parameters of every building project and guide clients through the process in creative, fun, and efficient way. These are the personal stories of just a few of our clients who've improved their lifestyle by downsizing, earning rental income, or just sharing extra space for work, family and guests by building a new backyard cottage, accessory dwelling unit, or tiny house.
  </p>

  <% @brochures.each do |c| %>
    <% if c.is_published %>
    <div class="row">
      <div class="large-3 columns small-12">
        <%= link_to brochure_client_with_slug_path(:slug => c.slug), :title => c.title, :class => "link-block hide-for-small" do %>
          <%= image_tag c.cover_image(:medium_square),:title => c.title, :alt => c.title %>
        <% end %>
      </div>
      <div class="large-9 columns small-12">

        <h4 class="hide-for-small"><%= link_to c.title, brochure_client_with_slug_path(:slug => c.slug) %></h4>

        <div class="row">
        <div class="small-4 columns show-for-small">
          <%= link_to brochure_client_with_slug_path(:slug => c.slug), :title => c.title, :class => "link-block" do %>
            <%= image_tag c.cover_image(:medium_square),:title => c.title, :alt => c.title %>
          <% end %>
        </div>
        <div class="small-8 columns show-for-small">
          <h4 class="show-for-small"><%= link_to c.title, brochure_client_with_slug_path(:slug => c.slug) %></h4>
          <ul>
            <li>Area, sq. ft.: <%= c.area %></li>
            <li># Bed: <%= c.number_of_bed %></li>
            <li># Bath: <%= c.number_of_bath %></li>
            <li>Has loft? <%= c.has_loft ? "Yes" : "No" %></li>
          </ul>
        </div>

        <div class="small-12 columns" style="margin-top: 1em;">
          <ul class="hide-for-small">
            <li>Area, sq. ft.: <%= c.area %></li>
            <li># Bed: <%= c.number_of_bed %></li>
            <li># Bath: <%= c.number_of_bath %></li>
            <li>Has loft? <%= c.has_loft ? "Yes" : "No" %></li>
          </ul>
        <%= simple_format c.short_description %>
        </div>
        </div>

      </div>
    </div>
    <hr> 
    <% end %>
  <% end %>


<% end %>

<% content_for :sidebar do %>
  <%= render :partial => 'frontend/inquiries/mad_lib_form' %>
<% end %>