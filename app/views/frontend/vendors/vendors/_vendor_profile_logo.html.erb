<div class="tile center-text">
  <% if vendor %>
    <%= link_to vendor_path(vendor) do %>
      <%= image_tag vendor_logo_for(vendor),:title => vendor.name, :alt => vendor.name %>
    <% end %>
  <% end %>
</div>