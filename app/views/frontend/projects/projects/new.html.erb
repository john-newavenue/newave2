<% provide(:title, 'Create a New Project') %>

<%= semantic_form_for @project, :as => :project, :url => projects_path do |f| %>
  <%= f.inputs "Tell us about your project" do %>
    <%= f.input :title  %>
    <%= f.input :description %>
  <% end %>
  <%= f.actions %>
<% end  %>  